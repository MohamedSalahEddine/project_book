
    <h2>All authors</h2>
    <form action="/authors" method="GET">
        <label>Name</label>
        <input type="text" name="name" value="<%= search_options.name %>">
        <button type="submit ">Search</button>
    </form>

    <% authors.forEach((element)=>{%>
        <div><%= element.name%></div>
    <% }) %>
